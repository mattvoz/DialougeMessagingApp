<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dialogue">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Utils.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.UserMessaging"></activity>
        <activity android:name=".ui.FriendsList_Activity" />
        <activity android:name=".ui.SignUp" />
        <activity android:name=".ui.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.CurrentMessages" />
        <activity android:name=".ui.userFirends_Activity" />
        <activity android:name=".ui.AdminTools"/>
        <activity android:name=".ui.friendRequest_Activity" />
    </application>

</manifest>
